class Solution {
public:
    int lengthOfLastWord(string s) {
        stringstream ss(s);
        string word;
        int length =0;
        while(ss >> word)
        {
         length = word.length();
        }
        return length;
    }
};
